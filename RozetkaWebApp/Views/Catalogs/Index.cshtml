@model IEnumerable<RozetkaWebApp.Models.Catalog>

@{
    ViewData["Title"] = "Index";
}

<h1>Каталоги</h1>

<p>
    <a asp-action="Create">Создать новый каталог</a>
</p>
<table class="table">
    <thead>
        <tr>
           @* <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Label)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attributes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Portal)
            </th>*@
             <th>
                ID
            </th>
            <th>
                Каталог
            </th>
            <th>
                Ресурсы
            </th>
            <th>
                Действия
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CatalogId)
            </td>
            <td>
                <a asp-controller="Products" asp-action="Index" asp-route-id="@item.CatalogId">@Html.DisplayFor(modelItem => item.Label)</a>
            </td>
           @* <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attributes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Portal.Label)
            </td>*@
             <td>
                <a asp-controller = "Properties" asp-action="Index" asp-route-id="@item.CatalogId">Свойства</a> |
                <a asp-controller = "CatalogImages" asp-action="Index" asp-route-id="@item.CatalogId">Изображения</a>    
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CatalogId"> Редактировать </a> |
                <a asp-action="Details" asp-route-id="@item.CatalogId"> Детали </a> |
                <a asp-action="Delete" asp-route-id="@item.CatalogId"> Удалить </a>
            </td>
        </tr>
}
    </tbody>
</table>
