@using RozetkaWebApp.App_GlobalResources;
@model RozetkaWebApp.Models.Portal

@{
    ViewBag.Title=(("Портал - "))+@Model.Label;
}
@{
    ViewBag.Portal = Model;
    ViewBag.DetailsTab = (("active"));
    Html.RenderPartial((("_PortalNavigator")));
}
     <p></p>
        <h4> @Resources.Информация_о_портале</h4>
     <p></p>

<div>

     <div class="form-group">
          <a class="btn btn-success" asp-action="Edit" asp-route-id="@Model.PortalId"> @Resources.Изменить </a>
          <a class="btn btn-danger" asp-action="Delete" asp-route-id="@Model.PortalId"> @Resources._Remove </a>     
           <button type="button" class="btn btn-primary"  onclick= "history.go(-1)">@Resources.Отменить</button>  
            <hr />
      </div>   

    <dl class="row">
        <dt class = "col-sm-2">
            @Resources.Заголовок0
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class = "col-sm-2">
            @Resources.Этикетка
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Label)
        </dd>
        <dt class = "col-sm-2">
            @Resources.Описание
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class = "col-sm-2">
            @Resources.Атрибуты
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Attributes)
        </dd>
    </dl>
</div>

<style>
  .card:hover {
    transition: transform 0.2s ;
    transform: scale(1.1);
  }
</style>

    <div class="row pt-3 mb-4">
        @foreach (var item in Model.Catalogs) 
        {
              <div class="col-lg-2 col-md-4 mb-4 mb-lg-2 d-flex align-items-stretch">
                 <div class="card rounded shadow-sm border-1 p-3 mb-2 ">
                    <div class="card-body p-0 m-0">
                      <img src="/images/Catalogs/@(item.CatalogId)/First" alt="" class="w-100 img-fluid d-block mx-auto">
                      <p></p>
                      <h5 class ="m-0">@Html.DisplayFor(modelItem => item.Label)</h5>
                      <p class="small text-muted">@Html.DisplayFor(modelItem => item.Title)</p>
                   </div> 
                  @* <ul class="list-inline small text-right mb-2">
                        <li class="list-inline-item"><a asp-action="Edit"   asp-route-id="@item."> <i class="fas fa-solid fa-pen-square"></i> </a> </li> 
                        <li class="list-inline-item"><a asp-action="Delete" asp-route-id="@item.RootImageId"> <i class="fas fa-solid fa-trash"></i> </a></li>
                   </ul>    *@            
                 </div> 
              </div>  
        }
    </div>
