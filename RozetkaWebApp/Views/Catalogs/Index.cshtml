@model IEnumerable<RozetkaWebApp.Models.Catalog>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Label)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attributes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Portal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Label)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attributes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Portal.Label)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CatalogId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CatalogId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CatalogId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
