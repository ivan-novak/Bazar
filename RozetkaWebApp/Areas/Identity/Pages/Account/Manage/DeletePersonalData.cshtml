@page
@model DeletePersonalDataModel
@{
    ViewBag.Title= "Delete Personal Data";
    ViewBag.ActivePage = ManageNavPages.PersonalData;
}

<h4>@ViewBag.Title</h4>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Удаление этих данных навсегда удалит вашу учетную запись, и это невозможно восстановить.</strong>
    </p>
</div>

<div>
    <form id="delete-user" method="post" class="form-group">
        <div asp-validation-summary="All" class="text-danger"></div>
        @if (Model.RequirePassword)
        {
            <div class="form-group">
                <label asp-for="Input.Password">Пароль</label>
                <input asp-for="Input.Password" class="form-control" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
        }
        <button class="btn btn-danger" type="submit">Удалить данные и мой аккаунт</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}